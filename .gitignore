# IDE and local-only state. The private companion repo (requirements, prompts,
# economics) is a separate checkout outside this tree, so nothing to ignore here.
.idea/
.claude/settings.local.json

# Secrets. .env.example is the template and is committed.
.env
.env.*
!.env.example

# Foundry
out/
cache/
broadcast/

# Rust
target/

# Node
node_modules/
dist/
*.tsbuildinfo

# Python
.venv/
__pycache__/
